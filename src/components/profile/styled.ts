import styled from "styled-components";

export const Wrapper = styled.main`
  display: flex;
  width: 90%;
  margin: 5rem auto;
  gap: 10rem;

  figure {
    display: flex;
    width: 40rem;
    height: 40rem;
    img {
      max-width: 100%;
    }
  }
`;
