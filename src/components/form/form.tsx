import Input from "./input"

const Form = () => {
  return (
    <form>
        <fieldset>
            <Input id="search" placeholder="Busque um Usuario..."/>
        </fieldset>
      </form>
  )
}

export default Form